
<div class="right-top-bar flex-w h-full">
    @if (Session["Taikhoan"] == null)
    {
        <a href="@Url.Action("Login","User")" class="flex-c-m trans-04 p-lr-25">
            Login
        </a>

        <a href="@Url.Action("Register","User")" class="flex-c-m trans-04 p-lr-25">
            Register
        </a>
    }
    else
    {
        <a href="#" class="flex-c-m trans-04 p-lr-25">
            Xin Chào @Session["HoTen"]
        </a>

        <a href="@Url.Action("Logout","User")" class="flex-c-m trans-04 p-lr-25">
            Log out
        </a>
    }
</div>
